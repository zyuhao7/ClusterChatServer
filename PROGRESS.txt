================================================================================
TASK ID: task-validate-final-agents-document
TASK NAME: Validate Final AGENTS Document
STATUS: complete
TIMESTAMP: 2026-04-10T09:19:13Z
WORKER AGENT: Worker-v2
================================================================================
OBJECTIVE (Acceptance Criteria):
   Final AGENTS.md must include only high-signal, repo-specific instructions.
   Commands must be executable from the repository context. Content must remain
   short enough for a quick ramp-up read.

STEPS PROVIDED:
   1. Cross-check each AGENTS.md command against its source file.
   2. Confirm each AGENTS.md line addresses a likely agent mistake.
   3. Remove ambiguous or duplicate wording from final text.

STEPS EXECUTED:
   ✓ Step 1: Cross-checked every AGENTS.md command against repository sources
     (README.md, CMakeLists.txt, scripts/, db/migrations/, config/, docker-compose.yml).
   ✓ Step 2: Confirmed each retained line targets common operational mistakes
     (wrong test path, migration order, config misuse, example workflow boundary).
   ✓ Step 3: Removed duplicate Docker command path and clarified run-port meaning;
     corrected test binary path from `./build/cluster_chat_unit` to `./bin/cluster_chat_unit`.

WORK COMPLETED:
   1. Validated command coverage and source mappings for build/run/test/db/docker sections.
   2. Tightened Docker section to one canonical command (`bash scripts/run_stack.sh`).
   3. Added concise port clarification for `chat_client` usage (`9120` direct, `8000` nginx).
   4. Fixed unit-test binary references to repository-true output path (`bin/cluster_chat_unit`).

FILES MODIFIED:
   - AGENTS.md (modified)
   - PROGRESS.txt (modified)

KEY DECISIONS:
   - Treated CMake output-path semantics and generated build artifacts as source-of-truth
     for executable paths.
   - Kept only concise, repo-specific instructions tied to real failure patterns.
   - Removed redundant command variants when a script wrapper already provides canonical flow.

BLOCKERS RESOLVED:
   - Resolved stale test-binary path mismatch by using `bin/cluster_chat_unit`.

REMAINING ISSUES:
   - None

NOTES FOR NEXT AGENT:
   - If CMake output settings change, re-validate AGENTS binary paths before editing docs.

ACCEPTANCE CRITERIA VERIFICATION:
   ✓ Criterion 1: Only high-signal, repo-specific instructions - VERIFIED
     (sections remain focused on project commands, boundaries, and known gotchas only).
   ✓ Criterion 2: Commands executable from repository context - VERIFIED
     (paths/commands mapped to README/CMake/scripts, and `bin/` artifacts confirmed present).
   ✓ Criterion 3: Short quick ramp-up read - VERIFIED
     (compact 71-line structure preserved with minimal wording).

TESTING RESULTS:
   ✓ Static verification: command-to-source mapping validated via grep/read checks
   ✓ Runtime artifact check: `/home/xh/ClusterChatServer/bin` contains
     `chat_server`, `chat_client`, `cluster_chat_unit`
   ✓ Consistency review: no ambiguous duplicate Docker command remains

================================================================================

================================================================================
TASK ID: task-draft-compact-agents-guidance
TASK NAME: Draft Compact AGENTS Guidance
STATUS: complete
TIMESTAMP: 2026-04-10T09:13:52Z
WORKER AGENT: Worker-v2
================================================================================
OBJECTIVE (Acceptance Criteria):
   AGENTS.md must stay compact and scan-friendly. Guidance must prioritize likely
   failure points such as wrong test paths and migration order. Every retained
   command must be verifiable from repository files.

STEPS PROVIDED:
   1. Keep short sections for build, run, test, database, docker, boundaries, gotchas.
   2. Replace stale command paths with verified command paths.
   3. Preserve useful existing guidance with current accuracy.
   4. Remove generic advice without repository-specific value.

STEPS EXECUTED:
   ✓ Step 1: Reorganized AGENTS.md into compact sections: build, run, test, database,
     docker, boundaries, gotchas.
   ✓ Step 2: Replaced/kept commands only after checking repo evidence in CMake files,
     README.md, scripts/, docker-compose.yml, db/migrations/, and tests/ paths.
   ✓ Step 3: Preserved accurate high-value guidance (config-file usage, installer
     boundary, example directory boundary).
   ✓ Step 4: Removed generic dependency/setup detail not tied to frequent repo-specific
     mistakes.

WORK COMPLETED:
   1. Rewrote AGENTS.md into a concise 71-line guide focused on execution-critical commands.
   2. Kept only repository-backed commands for build/run/test/database/docker workflows.
   3. Added/kept failure-point warnings for test path confusion and strict migration order.
   4. Tightened boundaries section to avoid misuse of install.sh and example workflow scope.

FILES MODIFIED:
   - AGENTS.md (modified)
   - PROGRESS.txt (modified)

KEY DECISIONS:
   - Used direct repository evidence as the source of truth for command retention.
   - Prioritized commands and notes that address common operational failures.
   - Kept the document compact by removing low-signal generic sections.

BLOCKERS RESOLVED:
   - Corrected boundary wording from "Makefile" to actual `example/makefile` naming.

REMAINING ISSUES:
   - None

NOTES FOR NEXT AGENT:
   - If AGENTS.md is updated again, re-verify each command path against repository files
     before retaining it.

ACCEPTANCE CRITERIA VERIFICATION:
   ✓ Criterion 1: AGENTS.md compact and scan-friendly - VERIFIED
     (reduced to short sectioned format with focused command blocks).
   ✓ Criterion 2: Prioritizes likely failure points - VERIFIED
     (explicit gotchas for wrong test path and migration order).
   ✓ Criterion 3: Every retained command verifiable from repository files - VERIFIED
     (checked against CMakeLists.txt, src/*/CMakeLists.txt, README.md, scripts/*,
      docker-compose.yml, db/migrations/*, tests/integration/*).

TESTING RESULTS:
   ✓ Static verification: command/path evidence validated against repository files
   ✓ Document review: section structure matches requested compact format

================================================================================
TASK ID: task-audit-high-signal-repository-sources
TASK NAME: Audit High-Signal Repository Sources
STATUS: complete
TIMESTAMP: 2026-04-10T09:07:12Z
WORKER AGENT: Worker-v2
================================================================================
OBJECTIVE (Acceptance Criteria):
   Audit output must list verified build, run, test, migration, and docker
   commands. Each extracted command must map to at least one repository file.
   Output must avoid speculative statements.

STEPS PROVIDED:
   1. Read README.md with top-level build instructions.
   2. Read CMakeLists.txt with target and option definitions.
   3. Read scripts and test configs with execution commands.
   4. Record only facts that have direct file evidence.

STEPS EXECUTED:
   ✓ Step 1: Read README.md and extracted build/run/test/migration/docker commands.
   ✓ Step 2: Read CMakeLists.txt and target CMake files for options/targets/output names.
   ✓ Step 3: Read scripts/*, tests/cpp/CMakeLists.txt, tests/integration/test_admin_service.py,
     docker-compose.yml, and Dockerfiles to verify execution commands/entrypoints.
   ✓ Step 4: Produced fact-only audit with file-backed command mappings.

WORK COMPLETED:
   1. Collected build commands from README.md and Dockerfile build stage.
   2. Verified executable entrypoints from src/server/CMakeLists.txt, src/client/CMakeLists.txt,
      docker-compose.yml, and Dockerfiles.
   3. Collected run/test/health-check commands from README.md and scripts/*.sh.
   4. Collected migration commands from README.md and script argument contract from
      scripts/migrate_passwords.py.
   5. Produced a non-speculative, file-evidence-backed audit summary.

FILES MODIFIED:
   - PROGRESS.txt (created)

KEY DECISIONS:
   - Treated "verified" as "directly present in repository files".
   - Used repository executable/config sources (CMake, scripts, Docker, test files) plus README
     command blocks as documented workflow commands.
   - Excluded environment-specific or inferred commands not explicitly present in files.

BLOCKERS RESOLVED:
   - None

REMAINING ISSUES:
   - None

NOTES FOR NEXT AGENT:
   - If stricter scope is needed, filter to executable/config files only and exclude README docs.
   - Current audit already includes direct mappings for every listed command.

ACCEPTANCE CRITERIA VERIFICATION:
   ✓ Criterion 1: List verified build/run/test/migration/docker commands - VERIFIED
     (commands extracted from README.md, CMake files, scripts, docker-compose.yml, Dockerfiles).
   ✓ Criterion 2: Each command maps to repository file evidence - VERIFIED
     (each command tied to at least one concrete file path).
   ✓ Criterion 3: Avoid speculative statements - VERIFIED
     (only explicit file content was reported; no inferred behavior beyond file definitions).

TESTING RESULTS:
   ✓ Verification method: static file-audit against repository sources
   ✓ Command execution tests: not applicable for this documentation audit task
   ✓ Consistency review: all listed commands are traceable to file evidence

================================================================================
